This project tries to mimic the course Microservices Architecture and implementation on .NET using node
to run it lunch
docker-compose -f docker-compose.yml -f docker-compose.override.yml up -d --build
